# ============================================================================
# Cinematic Agent Mesh - Backend Configuration
# ============================================================================
# REQUIRED: These must be set for the application to start
# OPTIONAL: These have sensible defaults if not specified

# === REQUIRED: API KEYS ===
# Concentrate AI Gateway API Key
# Get your key from: https://console.concentrate.ai
CONCENTRATE_API_KEY=your_concentrate_api_key_here

# TMDB (The Movie Database) API Key
# Get your key from: https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_api_key_here

# === OPTIONAL: API ENDPOINTS ===
# Leave these as-is unless you're using different endpoints
CONCENTRATE_BASE_URL=https://api.concentrate.ai/v1
TMDB_BASE_URL=https://api.themoviedb.org/3
TVMAZE_BASE_URL=https://api.tvmaze.com

# === OPTIONAL: DATABASE ===
# Path where SQLite database will be stored
# In Docker: /app/data/chat_history.db
# Locally: ./data/chat_history.db
DATABASE_PATH=/app/data/chat_history.db
DATABASE_POOL_SIZE=5

# === OPTIONAL: LOGGING ===
# Valid values: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# === OPTIONAL: AGENT CONFIGURATION ===
# Token limit for agent memory (affects conversation history length)
AGENT_MEMORY_TOKEN_LIMIT=4000

# Enable detailed agent reasoning output
AGENT_VERBOSE=true

# === OPTIONAL: NETWORK CONFIGURATION ===
# HTTP request timeout (seconds)
HTTP_TIMEOUT=30

# Number of retries for failed requests
HTTP_RETRIES=3

# Backoff factor for retries (exponential)
RETRY_BACKOFF_FACTOR=1.0

# === OPTIONAL: CORS CONFIGURATION ===
# Comma-separated list of allowed origins
# Note: Can be overridden in config.py if needed
ALLOWED_ORIGINS=http://localhost:8501,http://frontend:8501

# === OPTIONAL: API METADATA ===
API_TITLE=Cinematic Mesh API
API_VERSION=1.0.0
